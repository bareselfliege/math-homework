<?php
// Function to calculate the area of a rectangle
function getArea($length, $width) {
  return $length * $width;
}

// Test the function with different values
echo getArea(5, 3); // Should print "15"
echo getArea(2, 4); // Should print "8"
?>
